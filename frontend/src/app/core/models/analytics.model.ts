export interface DepartmentCompensationSummary {
  department: string;
  currency: string;
  headcount: number;
  averageSalary: number;
  medianSalary: number;
}

export interface CountryCompensationSummary {
  country: string;
  currency: string;
  headcount: number;
  averageSalary: number;
  medianSalary: number;
}
